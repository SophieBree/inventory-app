extends layout

block content
  h1= title
  if user 
    p Welcome back, !{user.username}
    p Your membership status is: !{user.membershipStatus}
  p Welcome to #[strong Local Bookshop], a place to browse and buy classic and contemporary books.

  if error
    p Error getting dynamic content.
  else
    p The bookshop has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}